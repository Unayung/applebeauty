require 'rails_helper'

RSpec.describe Link, type: :model do
  context "when created from rake job" do
    it "should have url"
    it "should have title"
    it "should have rate"
    it "rate should eq to 0"
  end
end
